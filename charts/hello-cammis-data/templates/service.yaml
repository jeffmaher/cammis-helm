apiVersion: v1
kind: Service
metadata:
  name: {{.Chart.Name}}-service
  labels:
    app: {{.Chart.Name}}
spec:
  type: LoadBalancer
  ports:
    - port: {{.Values.hello_cammis_data.port}}
      targetPort: {{.Values.hello_cammis_data.port}}
      protocol: TCP
  selector:
    app: {{.Chart.Name}}